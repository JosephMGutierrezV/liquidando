import { createAction, props } from '@ngrx/store';

export const nextStep = createAction('[Steps Fast Liquide] next step');
export const previousStep = createAction('[Steps Fast Liquide] previous step');
